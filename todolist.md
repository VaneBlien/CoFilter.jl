## CoFilter.jl 待做列表

---

### 算法扩展

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 高 | **矩阵分解 Builder** | 实现 SVD/ALS，处理稀疏矩阵，适合隐式反馈场景 |
| 高 | **基于内容 Builder** | 支持物品特征（类型、描述等），解决物品冷启动 |
| 中 | **时间衰减 Builder** | 包装现有 Builder，对新交互赋予更高权重 |
| 中 | **Penalty Builder** | 对热门物品降权，提升推荐多样性 |
| 低 | **二值反馈支持** | 除了评分，优化对点击/购买等隐式反馈的处理 |

### 性能优化

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 高 | **多进程分布式** | 真正利用 `Distributed.jl` 多 worker 并行计算相似度 |
| 中 | **GPU 加速** | 利用 CUDA.jl 加速矩阵乘法 |
| 中 | **增量更新优化** | 目前新增物品/用户走全量重算，可实现真正局部更新 |
| 低 | **内存优化** | 对极大矩阵的 out-of-core 处理 |

### 工程完善

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 高 | **序列化/反序列化** | 训练好的模型保存/加载，避免每次重算 |
| 高 | **日志系统** | 替换 `println`，支持分级和文件输出 |
| 中 | **配置系统** | TOML/YAML 配置文件驱动 |
| 中 | **REST API** | 用 HTTP.jl 封装推荐服务接口 |
| 低 | **文档完善** | Documenter.jl 自动生成 API 文档 |
| 低 | **注册为 Julia 包** | 提交到 JuliaRegistry，支持 `Pkg.add` |

### 测试与质量

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 高 | **MovieLens 全流程集成测试** | 下载→训练→评估→推荐，自动化 |
| 中 | **性能回归测试** | 确保新提交不拖慢速度 |
| 中 | **边缘场景补全** | 极大 n、空矩阵、纯零行等 |
| 低 | **代码覆盖率** | 目标 90%+ |

### 文档

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 高 | **examples 补全** | custom_metric.jl、movielens_demo.jl 完整示例 |
| 中 | **API 参考** | 每个公开函数的使用说明 |
| 低 | **性能调优指南** | TopK、batch_size 参数选择建议 |

---

**当前版本**：v0.1.0  
**测试覆盖**：225 tests, all passed  
**支持算法**：User-Based CF, Item-Based CF, Hybrid (WeightedSum/RoundRobin)  
**相似度度量**：Cosine, Pearson, Jaccard, AdjustedCosine